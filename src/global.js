const departId = ''
const openId = ''
const unionId = ''
const host = 'http://localhost:1626' //写自己的域名     https://oostest.zailaboratory.com      'http://localhost:1626'
const servicegoHost = 'https://servicego.udesk.cn'

export default {
    host, //host
    servicegoHost, //servicegoHost
    departId, //user所在部门的Id
    openId, //openId
    unionId, //unionId
}
